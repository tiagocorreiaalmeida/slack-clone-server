overwrite: true
schema: ./src/shared/infra/graphql/schema.ts
require:
  - ts-node/register/transpile-only
generates:
  ./src/shared/infra/graphql/generated/typed-schema.ts:
    plugins:
      - typescript
      - typescript-resolvers
